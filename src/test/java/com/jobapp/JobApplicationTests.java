package com.jobapp;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;



@SpringBootTest
class JobApplicationTests {

    @Test
    void contextLoads() {
      /*  Job job = new Job();
        job.setId(1);
        JobService jobService = mock(JobServiceImp.class);
        JobDao jobDao = mock(JobDao.class);
//        List<Job> expectedJobs = new ArrayList<>();
        when(jobDao.findById(1L)).thenReturn(Optional.of(job));
        Job job1 = jobService.getOneJob(1);
        assertThat(job1)
                .isEqualTo(job)
        ;

       */
    }

}
